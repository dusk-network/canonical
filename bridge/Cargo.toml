[package]
name = "bridge"
version = "0.1.0"
authors = ["Kristoffer Ström <kristoffer@rymdkoloni.se>"]
edition = "2018"

[dependencies]
canonical = { path = ".." }

[profile.release]
opt-level = 'z'
lto = true
codegen-units = 1
panic = 'abort'